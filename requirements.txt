# Python dependencies for ghost-processing
# System binaries also required (not pip-installable):
#   brew install sox ffmpeg   (Mac)
#   apt-get install sox ffmpeg (Docker — handled in Dockerfile)

audioop-lts==0.2.1    # Python 3.13 compatibility shim for pydub (audioop removed from stdlib)
psutil==5.9.8         # Child process management for graceful shutdown
pydub==0.25.1         # Audio decoding and silence detection
PyYAML==6.0.1         # Config file parsing
tqdm==4.66.4          # Progress bars
watchdog==4.0.1       # Filesystem event monitoring (watch mode)
